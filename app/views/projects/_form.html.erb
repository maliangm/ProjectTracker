<%= form_for @project do |f| %>
  <%= f.error_messages %>
  <table>
    <tr>
      <th><%= f.label :name %></th>
      <td><%= f.text_field :name %></td>
    </tr>
  </table>
  <%= f.submit %>
<% end %>
